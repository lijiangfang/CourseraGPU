Used Microsoft Copilot to help generate the `.jpeg` files for testing the model inference.

**test1:** Copilot generated 62 `.jpeg` files that look like handwritten characters.

**test2:** Copilot generated 62 `.jpeg` files with images inspired by the font **Patrick Hand**.

Under `test1`, you can find the evaluation outputs for each model and each mode. For example, `results1.txt` is the evaluation of **model1** in strict mode, and `results1-relaxed.txt` is the evaluation of **model1** in relaxed mode.